# Copy this file to .env and fill in your values, OR export these as real
# environment variables / put them in the systemd unit file.

BOT_TOKEN=123456789:AAExampleTokenFromBotFather
AUTHORIZED_USERS=123456789
LOG_TAIL_LINES=30

# Leave "true" if running via Docker (docker-compose.yml). Set to "false"
# if running directly on the host (Option B in README.md).
USE_NSENTER=true
